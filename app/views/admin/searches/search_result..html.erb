<h1>Public::Projects#index</h1>
<table class="table table-hover table-inverse">
  <!--検索対象モデルがUserの時 -->
  <% if @model_Member == "Members" %>
    <tbody>
      <% @members.each do |member| %>
        <tr>
          <td><%= member.member_name %></td>
        </tr>
      <% end %>
  <% end %>
    </tbody>
</table>
