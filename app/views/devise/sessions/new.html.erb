<div class="text-center">
  <div class="col-md-12">

    <% if flash[:alert] %>
      <div class="alert alert-danger">
      <%= flash[:alert] %>
      </div>
    <% end %>

  <!--管理者ログイン-->
      <%- if request.path.match(/\/admin(\/)?/) %>
        <h4><b>管理者ログイン</b></h4>

          <%= form_for(Admin.new, url: admin_session_path) do |f| %>

            <h6><b><%= f.label :メールアドレス %></b><br>
              <%= f.email_field :email, autofocus: true, autocomplete: "email" %></h6>

            <h6><b><%= f.label :パスワード %></b><br>
              <%= f.password_field :password, autocomplete: "current-password" %></h6>

            <h6><% if devise_mapping.rememberable? %>
                <%= f.check_box :remember_me %>
                <%= f.label :remember_me %>
              <% end %>
            </h6>
            <br>

            <%= f.submit "ログイン", class: "btn btn-success" %>

          <% end %><!--f-->
      <% end %><!--(/\/admin(\/)?/)-->
    </div>


    <!--メンバーログイン-->
    <%- if request.path.match(/\/members(\/)?/) %>

          <h4><b>メンバーログイン</b></h4>
        <div class="left-50px-pin">
          <p>管理者から付与されたメールアドレスとパスワードを</p>
          <p>ご入力ください</p>
       </div>

      <%#= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <%= form_for(Member.new, url: member_session_path) do |f| %>
        <h6><b><%= f.label :メールアドレス %></b><br>
        <%= f.email_field :email, autofocus: true, autocomplete: "email" %></h6>

        <h6><b><%= f.label :パスワード %><br />
        <%= f.password_field :password, autocomplete: "current-password" %></h6>


        <% if devise_mapping.rememberable? %>

          <h6><%= f.check_box :remember_me %>
          <%= f.label :remember_me %></h6>

        <% end %>
        <br>

          <%= f.submit "ログイン", class: "btn btn-success" %>

        <% end %><!--devise-->
      <% end %><!--(/\/members(\/)?-->

    <br>
    <p>※メンバーは管理者画面の「メンバー追加」から登録できます</p>
  </div><!--col-md-12-->
</div><!--text-center-->