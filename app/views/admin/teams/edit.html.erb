<h1>チーム名編集</h1>
<%= form_with model: @team, url:admin_team_path do |f| %>
  <h4>チーム名</h4>
  <%= f.text_field :team_name %>
  <%= f.submit '保存' %>
<% end %>

  　<%= link_to "このチームを削除", admin_teams_destroy_path(@team), class: "btn btn-secondary" %>
