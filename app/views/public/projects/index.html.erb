<h1>すべての企画一覧</h1>
<%# 現在の登録数を表示する。“#{}”は、Rubyの式展開 %>

<%# @albumsでレコードが1件もない場合の分岐 %>
<% if @projects.count == 0 %>
  <%# @albumsにデータがない場合の表示 %>
  <p>
    企画が追加されていません
  </p>
<% else %>
  <%# @albumsにレコードがある場合、タイトルを表示する %>
  <% @projects.each do |project| %>
    <p>
      <%= link_to project.project_name, public_project_path(project.id) %>
    </p>
  <% end %>
<% end %>
